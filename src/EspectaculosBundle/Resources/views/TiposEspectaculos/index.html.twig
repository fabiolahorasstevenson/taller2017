{% extends 'base.html.twig' %}

{% block body %}
<!-- Armar Home -->
<!-- Crear un container -->
<div class="container">
<div class="row">
	<div class="col-sm-8"> </div>
  	<div class="col-sm-4">
  	 	<a href= "{{ path('new_tipoEspectaculo') }}" class="btn btn-default" aria-label="Rigth Align">
  	 	<span class="glyphicon glyphicon-plus" aria-hidden="true"></span> </a> 
  	 </div>
</div>
<br>
<br>
<div class="row">
<div class="col-sm-8"> 
	<div class="panel panel-primary">
	 <!-- Default panel contents -->
	  <div class="panel-heading">Listado de tipos de espectáculos</div>
	 	 <div class="panel-body">
	    <p> </p>
	  </div>

	  <!-- Table -->
	   	<table class="table">
				<tr>
					<th>Tipo</th>
					<th>         </th>
				</tr>
				{% for tipo in tiposEspectaculos %}
				<tr>
					<td>{{ tipo.nombre }}</td>
					<td><a href= "{{ path('show_tipoEspectaculos', {'id': tipo.id}) }}" 
							class="btn btn-default" aria-label="Left Align">
	  					<span class="glyphicon glyphicon-pencil" aria-hidden="true"></span>
					</a></td>
				</tr>
				{% endfor %}	
	  	</table>
  	</div>
 </div> 	
</div>
</div>
{% endblock %} 